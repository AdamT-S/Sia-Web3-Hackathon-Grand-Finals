doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title login
    link(rel="stylesheet", href="login.css")
  
  body
    .main
      //- Log in page label and class
      .Login
        form
          label.Spacetext Spacetext
          label Log in
          input.login(type="email", name="email", placeholder="Email", required="")
          input.password(type="password", name="password", placeholder="Password", required="")
          button(type="submit") Log in
          button(type="") Sign up

    script(src="actions.js")